# --- Redirect all plain HTTP traffic to HTTPS (except /health) ---
server {
    listen 0.0.0.0:PORT_REPLACE;
    server_name _;

    # Allow Railway healthcheck over plain HTTP
    location = /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Redirect everything else to HTTPS
    location / {
        return 308 https://$host$request_uri;
    }
}

# --- Main HTTPS server (Railway terminates TLS and sets X-Forwarded-Proto) ---
server {
    listen 0.0.0.0:PORT_REPLACE;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # ---- Security headers ----
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' https: http: data: blob: 'unsafe-inline'" always;

    # ---- Static asset caching ----
    location ~* \.(css|js|gif|ico|jpeg|jpg|png|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # ---------- SPA ----------
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ---------- API (Laravel) ----------
    location /api/ {
        add_header Access-Control-Allow-Origin "https://availly.me" always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Origin, Content-Type, Accept, Authorization, X-Requested-With" always;
        add_header Access-Control-Allow-Credentials "true" always;

        if ($request_method = OPTIONS) {
            add_header Access-Control-Max-Age 1728000 always;
            add_header Content-Type "text/plain; charset=utf-8";
            add_header Content-Length 0;
            return 204;
        }

        try_files $uri $uri/ @php_api;
    }

    # never execute PHP from web root
    location ~ \.php$ { return 404; }

    # PHP-FPM upstream
    location @php_api {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME /usr/share/nginx/html/api/public/index.php;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
        fastcgi_param HTTP_AUTHORIZATION $http_authorization;
    }

    # deny sensitive files
    location ~ /\.(env|git) { deny all; }
}
